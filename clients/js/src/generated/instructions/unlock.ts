/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type TransactionSigner,
  type WritableAccount,
} from '@solana/web3.js';
import { PALADIN_LOCKUP_PROGRAM_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export type UnlockInstruction<
  TProgram extends string = typeof PALADIN_LOCKUP_PROGRAM_PROGRAM_ADDRESS,
  TAccountDepositorOwnerAccount extends string | IAccountMeta<string> = string,
  TAccountDepositorTokenAccount extends string | IAccountMeta<string> = string,
  TAccountLockupAccount extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountDepositorOwnerAccount extends string
        ? ReadonlySignerAccount<TAccountDepositorOwnerAccount> &
            IAccountSignerMeta<TAccountDepositorOwnerAccount>
        : TAccountDepositorOwnerAccount,
      TAccountDepositorTokenAccount extends string
        ? ReadonlyAccount<TAccountDepositorTokenAccount>
        : TAccountDepositorTokenAccount,
      TAccountLockupAccount extends string
        ? WritableAccount<TAccountLockupAccount>
        : TAccountLockupAccount,
      ...TRemainingAccounts,
    ]
  >;

export type UnlockInstructionData = { discriminator: number };

export type UnlockInstructionDataArgs = {};

export function getUnlockInstructionDataEncoder(): Encoder<UnlockInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', getU8Encoder()]]),
    (value) => ({ ...value, discriminator: 1 })
  );
}

export function getUnlockInstructionDataDecoder(): Decoder<UnlockInstructionData> {
  return getStructDecoder([['discriminator', getU8Decoder()]]);
}

export function getUnlockInstructionDataCodec(): Codec<
  UnlockInstructionDataArgs,
  UnlockInstructionData
> {
  return combineCodec(
    getUnlockInstructionDataEncoder(),
    getUnlockInstructionDataDecoder()
  );
}

export type UnlockInput<
  TAccountDepositorOwnerAccount extends string = string,
  TAccountDepositorTokenAccount extends string = string,
  TAccountLockupAccount extends string = string,
> = {
  depositorOwnerAccount: TransactionSigner<TAccountDepositorOwnerAccount>;
  depositorTokenAccount: Address<TAccountDepositorTokenAccount>;
  lockupAccount: Address<TAccountLockupAccount>;
};

export function getUnlockInstruction<
  TAccountDepositorOwnerAccount extends string,
  TAccountDepositorTokenAccount extends string,
  TAccountLockupAccount extends string,
>(
  input: UnlockInput<
    TAccountDepositorOwnerAccount,
    TAccountDepositorTokenAccount,
    TAccountLockupAccount
  >
): UnlockInstruction<
  typeof PALADIN_LOCKUP_PROGRAM_PROGRAM_ADDRESS,
  TAccountDepositorOwnerAccount,
  TAccountDepositorTokenAccount,
  TAccountLockupAccount
> {
  // Program address.
  const programAddress = PALADIN_LOCKUP_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    depositorOwnerAccount: {
      value: input.depositorOwnerAccount ?? null,
      isWritable: false,
    },
    depositorTokenAccount: {
      value: input.depositorTokenAccount ?? null,
      isWritable: false,
    },
    lockupAccount: { value: input.lockupAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.depositorOwnerAccount),
      getAccountMeta(accounts.depositorTokenAccount),
      getAccountMeta(accounts.lockupAccount),
    ],
    programAddress,
    data: getUnlockInstructionDataEncoder().encode({}),
  } as UnlockInstruction<
    typeof PALADIN_LOCKUP_PROGRAM_PROGRAM_ADDRESS,
    TAccountDepositorOwnerAccount,
    TAccountDepositorTokenAccount,
    TAccountLockupAccount
  >;

  return instruction;
}

export type ParsedUnlockInstruction<
  TProgram extends string = typeof PALADIN_LOCKUP_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    depositorOwnerAccount: TAccountMetas[0];
    depositorTokenAccount: TAccountMetas[1];
    lockupAccount: TAccountMetas[2];
  };
  data: UnlockInstructionData;
};

export function parseUnlockInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedUnlockInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      depositorOwnerAccount: getNextAccount(),
      depositorTokenAccount: getNextAccount(),
      lockupAccount: getNextAccount(),
    },
    data: getUnlockInstructionDataDecoder().decode(instruction.data),
  };
}
