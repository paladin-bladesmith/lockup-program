/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/kinobi-so/kinobi
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
} from '@solana/web3.js';
import { PALADIN_LOCKUP_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export type InitializeLockupPoolInstruction<
  TProgram extends string = typeof PALADIN_LOCKUP_PROGRAM_ADDRESS,
  TAccountLockupPool extends string | IAccountMeta<string> = string,
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountLockupPool extends string
        ? ReadonlyAccount<TAccountLockupPool>
        : TAccountLockupPool,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeLockupPoolInstructionData = { discriminator: number };

export type InitializeLockupPoolInstructionDataArgs = {};

export function getInitializeLockupPoolInstructionDataEncoder(): Encoder<InitializeLockupPoolInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', getU8Encoder()]]),
    (value) => ({ ...value, discriminator: 0 })
  );
}

export function getInitializeLockupPoolInstructionDataDecoder(): Decoder<InitializeLockupPoolInstructionData> {
  return getStructDecoder([['discriminator', getU8Decoder()]]);
}

export function getInitializeLockupPoolInstructionDataCodec(): Codec<
  InitializeLockupPoolInstructionDataArgs,
  InitializeLockupPoolInstructionData
> {
  return combineCodec(
    getInitializeLockupPoolInstructionDataEncoder(),
    getInitializeLockupPoolInstructionDataDecoder()
  );
}

export type InitializeLockupPoolInput<
  TAccountLockupPool extends string = string,
> = {
  /** Lockup pool */
  lockupPool: Address<TAccountLockupPool>;
};

export function getInitializeLockupPoolInstruction<
  TAccountLockupPool extends string,
>(
  input: InitializeLockupPoolInput<TAccountLockupPool>
): InitializeLockupPoolInstruction<
  typeof PALADIN_LOCKUP_PROGRAM_ADDRESS,
  TAccountLockupPool
> {
  // Program address.
  const programAddress = PALADIN_LOCKUP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    lockupPool: { value: input.lockupPool ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [getAccountMeta(accounts.lockupPool)],
    programAddress,
    data: getInitializeLockupPoolInstructionDataEncoder().encode({}),
  } as InitializeLockupPoolInstruction<
    typeof PALADIN_LOCKUP_PROGRAM_ADDRESS,
    TAccountLockupPool
  >;

  return instruction;
}

export type ParsedInitializeLockupPoolInstruction<
  TProgram extends string = typeof PALADIN_LOCKUP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Lockup pool */
    lockupPool: TAccountMetas[0];
  };
  data: InitializeLockupPoolInstructionData;
};

export function parseInitializeLockupPoolInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedInitializeLockupPoolInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 1) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      lockupPool: getNextAccount(),
    },
    data: getInitializeLockupPoolInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
